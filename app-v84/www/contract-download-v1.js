(function(){
'use strict';
let contracts=[],loading=false,lastLoad=0;
const headers=()=>({apikey:cfg.supabasePublishableKey,Authorization:'Bearer '+token,'Content-Type':'application/json'});
async function refresh(){if(loading||Date.now()-lastLoad<3000)return;loading=true;try{const r=await fetch(cfg.supabaseUrl+'/rest/v1/rpc/app_list_contracts',{method:'POST',headers:headers(),body:'{}'});if(r.ok){contracts=await r.json();lastLoad=Date.now()}}finally{loading=false}}
async function download(id){const c=contracts.find(x=>x.id===id);if(!c?.pdf_path)return alert('Este contrato todavía no tiene PDF guardado.');const safe=c.pdf_path.split('/').map(encodeURIComponent).join('/'),urls=[cfg.supabaseUrl+'/storage/v1/object/authenticated/contracts/'+safe,cfg.supabaseUrl+'/storage/v1/object/contracts/'+safe];for(const url of urls){const r=await fetch(url,{headers:headers()});if(!r.ok)continue;const blob=await r.blob(),objectUrl=URL.createObjectURL(blob),a=document.createElement('a');a.href=objectUrl;a.download=(c.contract_number||'contrato')+'.pdf';document.body.appendChild(a);a.click();a.remove();setTimeout(()=>URL.revokeObjectURL(objectUrl),30000);return}alert('No se pudo recuperar el PDF guardado.')}
async function decorate(){const panel=document.getElementById('lrContractPanel');if(!panel||panel.classList.contains('hidden'))return;await refresh();document.querySelectorAll('.lrCpRow').forEach(row=>{if(row.querySelector('.lrCpDownload'))return;const number=row.querySelector('header b')?.textContent?.trim(),c=contracts.find(x=>String(x.contract_number||'').trim()===number);if(!c?.pdf_path)return;const actions=row.querySelector('.lrCpActions');if(!actions)return;const b=document.createElement('button');b.className='lrCpDownload';b.textContent='Descargar PDF';b.onclick=()=>download(c.id);actions.prepend(b)})}
const s=document.createElement('style');s.textContent='.lrCpDownload{background:#1d4ed8!important;color:#fff!important}.lrCpActions{gap:7px;flex-wrap:wrap}';document.head.appendChild(s);setInterval(decorate,1200);window.LariosContractDownload={download,refresh};
})();
