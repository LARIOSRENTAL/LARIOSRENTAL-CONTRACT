window.LariosScanner=(function(){let target='identity',lastFile=null;const $=id=>document.getElementById(id);function open(kind){target=kind;$('scanTitle').textContent=kind==='keys'?'Fotografiar llavero':'Escanear documentación';$('scanHelp').textContent=kind==='keys'?'Haz una foto nítida de la etiqueta de las llaves.':'Haz una foto nítida del DNI, pasaporte o permiso de conducir.';$('scan').classList.remove('hidden');$('reservation').classList.add('hidden')}function close(){$('scan').classList.add('hidden');$('reservation').classList.remove('hidden')}function choose(){const i=$('scanFile');i.value='';i.click()}async function selected(input){const f=input.files&&input.files[0];if(!f)return;lastFile=f;$('scanPreview').src=URL.createObjectURL(f);$('scanPreview').classList.remove('hidden');$('scanState').textContent='Imagen capturada. Puedes guardarla y rellenar los datos detectados.'}async function upload(){if(!lastFile)return alert('Primero haz una foto.');try{const ext=(lastFile.name.split('.').pop()||'jpg').toLowerCase();const path='mobile/'+new Date().toISOString().slice(0,10)+'/'+crypto.randomUUID()+'.'+ext;const r=await fetch(cfg.supabaseUrl+'/storage/v1/object/documents/'+path,{method:'POST',headers:{apikey:cfg.supabasePublishableKey,Authorization:'Bearer '+token,'Content-Type':lastFile.type||'image/jpeg','x-upsert':'false'},body:lastFile});if(!r.ok)throw new Error(await r.text());$('scanState').textContent='Foto guardada de forma privada en Supabase Storage.';return path}catch(e){alert('No se pudo guardar la imagen: '+e.message);return null}}function apply(){const text=($('scanText').value||'').trim();if(!text)return alert('Pega o dicta el texto leído del documento para aplicar los datos.');const lines=text.split(/\n+/).map(x=>x.trim()).filter(Boolean);if(target==='keys'){const plate=text.match(/\b\d{4}\s*[BCDFGHJKLMNPRSTVWXYZ]{3}\b/i);if(plate)$('vehicle_plate').value=plate[0].replace(/\s/g,'').toUpperCase();const group=text.match(/(?:GRUPO|GROUP|G)\s*[:\-]?\s*([A-Z0-9]{1,5})/i);if(group)$('vehicle_group').value=group[1].toUpperCase();$('vehicle_model').value=$('vehicle_model').value||lines[0]||''}else{const dni=text.match(/\b\d{8}[A-Z]\b/i);const nie=text.match(/\b[XYZ]\d{7}[A-Z]\b/i);const passport=text.match(/\b[A-Z]{1,3}\d{5,9}\b/i);if(dni||nie||passport)$('customer_document').value=(dni||nie||passport)[0].toUpperCase();const email=text.match(/[\w.+-]+@[\w.-]+\.[A-Z]{2,}/i);if(email)$('customer_email').value=email[0];const licence=text.match(/(?:PERMISO|LICEN[CS]E|DRIVING)[^A-Z0-9]*([A-Z0-9-]{5,20})/i);if(licence)$('driving_license').value=licence[1];if(!$('customer_name').value&&lines.length)$('customer_name').value=lines[0]}close()}return{open,close,choose,selected,upload,apply}})();