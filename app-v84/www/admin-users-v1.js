(function(){
'use strict';
const $=id=>document.getElementById(id);let users=[],currentUserId='',installed=false,saving=false;
const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
function admin(){return !!window.LariosAccess?.isAdmin?.()}
function headers(){return{apikey:cfg.supabasePublishableKey,Authorization:'Bearer '+token,'Content-Type':'application/json'}}
async function sessionFetch(url,options={},retry=true){const r=await fetch(url,options);if(retry&&(r.status===401||r.status===403)&&typeof window.refreshSession==='function'&&await window.refreshSession())return sessionFetch(url,{...options,headers:{...(options.headers||{}),Authorization:'Bearer '+token}},false);return r}
async function edge(action,extra={}){const r=await sessionFetch(cfg.supabaseUrl+'/functions/v1/admin-users',{method:'POST',headers:headers(),body:JSON.stringify({action,...extra})});const data=await r.json().catch(()=>({}));if(!r.ok)throw Object.assign(new Error(data.message||data.error||'No se pudieron gestionar los usuarios'),{data,status:r.status});return data}
const roleLabel=role=>role==='admin'?'Administrador':role==='employee'?'Empleado':'Sin acceso';
const lastSeen=value=>value?new Date(value).toLocaleString('es-ES',{dateStyle:'short',timeStyle:'short'}):'Nunca';
function ensureCard(){let card=$('lrAdminUsersCard');if(!admin()){card?.remove();return}if(card)return;const grid=document.querySelector('#home .grid');if(!grid)return;card=document.createElement('button');card.id='lrAdminUsersCard';card.className='card wide lrAdminUsersCard';card.type='button';card.dataset.adminOnly='true';card.onclick=open;card.innerHTML='<b>Usuarios y permisos</b><span>Añadir, cambiar permisos, recuperar contraseñas o eliminar cuentas</span>';grid.appendChild(card)}
function row(user){const own=user.id===currentUserId;return`<article class="lrAuRow"><div class="lrAuIdentity"><b>${esc(user.email||'Usuario sin email')}</b><span>Último acceso: ${esc(lastSeen(user.last_sign_in_at))}</span>${own?'<small>Tu cuenta administradora · protegida</small>':''}</div><select id="lrAuRole-${esc(user.id)}" ${own?'disabled':''} aria-label="Permiso de ${esc(user.email)}"><option value="none" ${user.role==='none'?'selected':''}>Sin acceso</option><option value="employee" ${user.role==='employee'?'selected':''}>Empleado</option><option value="admin" ${user.role==='admin'?'selected':''}>Administrador</option></select><div class="lrAuActions"><button type="button" data-admin-only="true" ${own?'disabled':''} onclick="LariosAdminUsers.save('${esc(user.id)}')">Guardar permiso</button><button type="button" class="reset" data-admin-only="true" onclick="LariosAdminUsers.reset('${esc(user.id)}')">Restablecer contraseña</button><button type="button" class="danger" data-admin-only="true" ${own?'disabled':''} onclick="LariosAdminUsers.remove('${esc(user.id)}')">Eliminar usuario</button></div></article>`}
function render(){const body=$('lrAdminUsersBody');if(!body)return;body.innerHTML=`<div class="lrAuNotice"><b>Gestión segura de usuarios</b><span>Las contraseñas nunca se muestran. Las altas y recuperaciones se completan mediante un enlace enviado al email de cada persona.</span></div><form class="lrAuInvite" onsubmit="LariosAdminUsers.invite();return false"><div><b>Añadir usuario</b><span>Recibirá una invitación para crear su contraseña.</span></div><input id="lrAuInviteEmail" type="email" autocomplete="off" placeholder="correo@empresa.com" required><select id="lrAuInviteRole"><option value="employee">Empleado</option><option value="admin">Administrador</option></select><button type="submit" data-admin-only="true">Enviar invitación</button></form>${users.map(row).join('')}<p class="lrAuFoot">Los cambios de permiso se aplican cuando la otra cuenta renueve su sesión. No puedes eliminar tu propia cuenta ni dejar el sistema sin administradores.</p>`}
async function load(){const body=$('lrAdminUsersBody');if(body)body.innerHTML='<div class="notice">Cargando usuarios…</div>';try{const data=await edge('list');users=Array.isArray(data.users)?data.users:[];currentUserId=String(data.current_user_id||'');render()}catch(error){if(body)body.innerHTML='<div class="notice lrAuError">'+esc(error.message)+'</div>'}}
function open(){if(!admin())return window.LariosAccess?.requireAdmin?.('gestionar usuarios');$('lrAdminUsersPanel')?.classList.remove('hidden');document.body.style.overflow='hidden';load()}
function close(){$('lrAdminUsersPanel')?.classList.add('hidden');document.body.style.overflow=''}
async function invite(){if(saving||!admin())return;const email=$('lrAuInviteEmail')?.value.trim(),role=$('lrAuInviteRole')?.value||'employee';if(!email)return alert('Indica el email del nuevo usuario.');if(!confirm('Enviar una invitación a '+email+' con permiso de '+roleLabel(role)+'?'))return;saving=true;try{const data=await edge('invite_user',{email,role});alert(data.message||'Invitación enviada.');await load()}catch(error){alert('No se envió la invitación. '+error.message)}finally{saving=false}}
async function save(userId){if(saving||!admin())return;const user=users.find(item=>item.id===userId),select=$('lrAuRole-'+userId);if(!user||!select)return;const next=select.value;if(next===user.role)return alert('Ese usuario ya tiene el permiso '+roleLabel(next)+'.');if(!confirm('Cambiar '+user.email+' de “'+roleLabel(user.role)+'” a “'+roleLabel(next)+'”?')){select.value=user.role;return}saving=true;try{const data=await edge('set_role',{user_id:userId,role:next});alert(data.message||'Permiso actualizado.');await load()}catch(error){select.value=user.role;alert('No se cambió el permiso. '+error.message)}finally{saving=false}}
async function reset(userId){if(saving||!admin())return;const user=users.find(item=>item.id===userId);if(!user)return;if(!confirm('Enviar a '+user.email+' un enlace para crear una contraseña nueva?'))return;saving=true;try{const data=await edge('reset_password',{user_id:userId});alert(data.message||'Enlace enviado.')}catch(error){alert('No se pudo enviar el enlace. '+error.message)}finally{saving=false}}
async function remove(userId){if(saving||!admin())return;const user=users.find(item=>item.id===userId);if(!user)return;if(!confirm('Eliminar la cuenta '+user.email+'? Ya no podrá volver a iniciar sesión.'))return;if(!confirm('Última confirmación: esta acción elimina definitivamente el acceso de '+user.email+'. ¿Continuar?'))return;saving=true;try{const data=await edge('delete_user',{user_id:userId});alert(data.message||'Usuario eliminado.');await load()}catch(error){alert('No se eliminó el usuario. '+error.message)}finally{saving=false}}
function install(){if(installed)return;installed=true;const panel=document.createElement('section');panel.id='lrAdminUsersPanel';panel.className='lrAdminUsersPanel hidden';panel.innerHTML='<div class="lrAuShell"><div class="lrAuHead"><button type="button" onclick="LariosAdminUsers.close()">←</button><div><b>Usuarios y permisos</b><span>Solo administradores</span></div><button type="button" onclick="LariosAdminUsers.reload()">Actualizar</button></div><div id="lrAdminUsersBody"></div></div>';document.body.appendChild(panel);const style=document.createElement('style');style.textContent='.lrAdminUsersCard{border-color:#c4b5fd!important;background:#faf5ff!important}.lrAdminUsersCard b{color:#5b21b6}.lrAdminUsersPanel{position:fixed;inset:0;z-index:100520;background:#f4f6f8;overflow:auto;padding-top:env(safe-area-inset-top)}.lrAuShell{max-width:980px;min-height:100%;margin:auto;background:#fff;padding:18px}.lrAuHead{display:flex;align-items:center;gap:12px;padding-bottom:14px;border-bottom:1px solid #e5e7eb}.lrAuHead>div{display:grid;flex:1}.lrAuHead b{font-size:21px}.lrAuHead span,.lrAuRow span,.lrAuRow small,.lrAuFoot,.lrAuInvite span{font-size:12px;color:#6b7280}.lrAuHead button,.lrAuRow button,.lrAuInvite button{padding:10px 12px;border:0;border-radius:9px;font-weight:800}.lrAuNotice{display:grid;gap:4px;margin:14px 0;padding:13px;border:1px solid #c4b5fd;background:#faf5ff;color:#5b21b6;border-radius:12px}.lrAuNotice span{font-size:12px;line-height:1.4}.lrAuInvite{display:grid;grid-template-columns:minmax(180px,1fr) minmax(220px,1.4fr) 160px 150px;gap:9px;align-items:center;padding:14px;margin:10px 0 16px;border:1px solid #86efac;background:#f0fdf4;border-radius:12px}.lrAuInvite>div,.lrAuIdentity{display:grid;gap:3px}.lrAuInvite input,.lrAuInvite select{margin:0}.lrAuInvite button{background:#166534;color:#fff}.lrAuRow{display:grid;grid-template-columns:minmax(200px,1fr) 170px minmax(330px,1.4fr);gap:10px;align-items:center;padding:13px 0;border-bottom:1px solid #e5e7eb}.lrAuRow select{margin:0}.lrAuActions{display:flex;gap:6px;flex-wrap:wrap}.lrAuRow button{background:#5b21b6;color:#fff}.lrAuRow button.reset{background:#1d4ed8}.lrAuRow button.danger{background:#fff;color:#b91c1c;border:1px solid #ef4444}.lrAuRow button:disabled{background:#e5e7eb;color:#6b7280;border-color:#e5e7eb}.lrAuError{color:#b91c1c}.lrAuFoot{line-height:1.5}@media(max-width:760px){.lrAuInvite,.lrAuRow{grid-template-columns:1fr}.lrAuActions button,.lrAuInvite button{width:100%}.lrAuShell{padding:12px}}';document.head.appendChild(style);window.addEventListener('larios:access-ready',ensureCard);ensureCard()}
window.LariosAdminUsers={open,close,reload:load,invite,save,reset,remove,__test:{roleLabel,setUsers:(value,id)=>{users=value;currentUserId=id}}};if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',install);else install();
})();
